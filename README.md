# Antipattern
Node.js Cli Web scraping tool that can intercept exposed api url or it can even fetch data from those exposed api.It can also extract from CSS selector and it is smart enough to handle the list of elements ( div soups , table tab )  
